/* @flow */

import React from 'react'
import WineContainer from '@Wine/containers/WineContainer'

type Props = {}

const WineScene = (props: Props): React$Element => {
  return (
    <WineContainer {...props} />
  )
}

export default WineScene
