/* @flow */

import React from 'react'
import styles from './Title'

const Title = (): React$Element<any> => {
  return (
    <h1 className={styles.text}>
      Title
    </h1>
  )
}

export default Title
